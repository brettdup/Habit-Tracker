🗓 2026-03-24 · 15:08 (GMT+2)

🔹 Request

User reported that the SwiftUI preview for `HabitListView.swift` was crashing during canvas updates with an abort trap.

🔧 Changes Made
	•	Guarded app launch side effects during Xcode previews so notification setup and authorization requests do not run inside the preview host.
	•	Refactored `ContentView_Previews` to use a fresh in-memory Core Data controller per preview render instead of clearing and repopulating shared preview state.
	•	Removed destructive preview data reset logic that could run repeatedly during preview refresh cycles.

Files modified:
	•	Habit Tracker/Habit_TrackerApp.swift
	•	Habit Tracker/HabitListView.swift

✅ Result

The preview path now uses isolated sample data and avoids app-level runtime side effects, reducing the main causes of preview-host crashes and making `HabitListView` previews stable during refreshes.
🗓 2026-03-24 · 15:11 (GMT+2)

🔹 Request

User provided a second preview crash report showing the canvas still aborting after the initial preview isolation changes.

🔧 Changes Made
	•	Added a dedicated preview runtime check inside `HabitListView.swift`.
	•	Disabled daily reset lifecycle work while SwiftUI previews are running.
	•	Fell back from the iOS 26 `glassEffect` modifier to the material background path during previews to avoid preview-host rendering instability.

Files modified:
	•	Habit Tracker/HabitListView.swift

✅ Result

Preview rendering now avoids both lifecycle mutation and the most aggressive preview-only visual effect path, further reducing causes of canvas aborts when `HabitListView` loads.
