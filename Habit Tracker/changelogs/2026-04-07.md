🗓 2026-04-07 · 12:06 (GMT+2)

🔹 Request

User requested a real default message for random nudges instead of leaving the message blank and falling back to the nudge title.

🔧 Changes Made
- Added a shared random nudge default message constant in `HabitDataServices.swift`.
- Updated random nudge notification scheduling to use that default message when a nudge message is blank.
- Updated the random nudge form in `RandomNudgesView.swift` to prefill the message field with the same default text for new nudges and for older nudges that had no custom message saved.
- Removed the `optional` wording from the message field placeholder since the form now provides a default value.

Files modified:
- `Habit Tracker/HabitDataServices.swift`
- `Habit Tracker/RandomNudgesView.swift`

✅ Result

Random nudges now use a consistent default body message both in the form and at notification-delivery time, instead of just echoing the nudge title.

⸻

🗓 2026-04-07 · 11:54 (GMT+2)

🔹 Request

User requested that random nudge notifications should be cleared from Notification Center when the app becomes active and when a nudge is tapped.

🔧 Changes Made
- Added delivered random nudge notification cleanup in `Habit_TrackerApp.swift` when the app becomes active.
- Added the same cleanup when a random nudge notification response is received.
- Kept the rolling nudge refill behavior after cleanup so nudges continue functioning normally.

Files modified:
- `Habit Tracker/Habit_TrackerApp.swift`

✅ Result

Delivered random nudges should no longer linger in Notification Center once the app is foregrounded or the user opens a nudge.

⸻

🗓 2026-04-07 · 11:50 (GMT+2)

🔹 Request

User reported that reminders were still becoming unscheduled after adding random nudges and asked for the History redesign to be reverted.

🔧 Changes Made
- Converted random nudges to a rolling queue model in `HabitDataServices.swift`, keeping only the next three pending nudges per nudge instead of preloading whole future days.
- Added nudge queue refill triggers in `Habit_TrackerApp.swift` so the app tops nudges back up when it becomes active and when a random nudge is delivered or interacted with.
- Reverted `HistoryView.swift` back to the previous grouped-list-based history screen.

Files modified:
- `Habit Tracker/HabitDataServices.swift`
- `Habit Tracker/Habit_TrackerApp.swift`
- `Habit Tracker/HistoryView.swift`

✅ Result

Random nudges should now consume far fewer pending notification slots while still refilling over time, reducing the chance that they crowd out habit reminders, and the History screen is back to the prior version.

⸻

🗓 2026-04-07 · 11:47 (GMT+2)

🔹 Request

Build error reported after the History redesign because `HabitDetailView` could no longer find the shared `HistoryDotsSection`.

🔧 Changes Made
- Restored `HistoryDotsSection` as a shared reusable view in `HistoryView.swift`.
- Kept the new History screen redesign while preserving compatibility with `HabitDetailView`, which also uses the dot-grid summary component.

Files modified:
- `Habit Tracker/HistoryView.swift`

✅ Result

The shared history dot-grid component is available again for both the main History screen and the habit detail history screen.

⸻

🗓 2026-04-07 · 11:45 (GMT+2)

🔹 Request

User reported that after adding random nudges, the Reminders screen could incorrectly show `Pending in iOS: none`.

🔧 Changes Made
- Updated `ScheduledNotificationsView` to use a settled notification refresh sequence instead of reading pending requests only immediately after nudge add/edit sheet dismissal.
- Applied the same settled refresh sequence to `RandomNudgesView` so both screens wait for the iOS pending notification queue to catch up after random nudge scheduling changes.
- Updated random nudge removal flows in both screens to use the same delayed re-read pattern.

Files modified:
- `Habit Tracker/ScheduledNotificationsView.swift`
- `Habit Tracker/RandomNudgesView.swift`

✅ Result

Adding, editing, or removing random nudges should no longer transiently knock the habit reminder verification lines into a false `Pending in iOS: none` state just because the queue was read too early.

⸻

🗓 2026-04-07 · 11:44 (GMT+2)

🔹 Request

User requested that the History view should feel much cooler and more unique.

🔧 Changes Made
- Rebuilt `HistoryView` from a standard grouped list into a more editorial layout with:
- a large hero summary card
- a horizontally scrollable recent-day session rail
- a focused selected-day detail panel with progress messaging
- richer completion cards and a missed-habits summary card
- a lighter archive section for older tracked days
- Added day selection state so users can inspect a specific recent or archived day instead of only reading chronological sections.
- Kept the existing Core Data-backed history model and delete capability while changing the presentation substantially.
- Adjusted the history refresh observer to use the broader-compatible single-parameter `onChange` form.

Files modified:
- `Habit Tracker/HistoryView.swift`

✅ Result

The History screen now feels like a distinctive activity dashboard rather than a plain log, while still preserving the app’s completion data and deletion flows.

⸻

🗓 2026-04-07 · 11:41 (GMT+2)

🔹 Request

User reported that the tappable date area did not look clickable enough, then disliked both the calendar icon and the chevron cue.

🔧 Changes Made
- Updated the center date-summary control in `HabitListView` to look like a button.
- Added a rounded background treatment, then removed the icon affordance entirely after feedback.
- Increased the tap target height while preserving the existing layout structure.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

The date summary area now looks visibly interactive, making it clearer that tapping it opens the calendar picker.

⸻

🗓 2026-04-07 · 11:34 (GMT+2)

🔹 Request

User requested that tapping the date summary area should open a calendar picker.

🔧 Changes Made
- Added a tappable action to the center date-summary area in `HabitListView`.
- Added a sheet with a graphical `DatePicker` so users can jump directly to any date.
- Kept the existing left/right day navigation buttons alongside the new picker flow.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Users can now tap the date header area to open a calendar picker and navigate directly to a chosen day.

⸻

🗓 2026-04-07 · 11:39 (GMT+2)

🔹 Request

User requested that the Habits screen should always show totals such as `4/20`, or `16 left` when using the `Not Done` filter.

🔧 Changes Made
- Added selected-day progress summary logic to `HabitListView`.
- Updated the date subtitle in the day navigator to append completion progress for the selected day.
- `All` mode now shows `done/total`, while `Not Done` mode shows the remaining count as `X left`.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

The day navigator now always shows progress context for the selected day, so the user can see completion totals at a glance regardless of the current filter mode.

⸻

🗓 2026-04-07 · 11:36 (GMT+2)

🔹 Request

User requested an option to show all habits for the selected day or only the ones not yet done.

🔧 Changes Made
- Added a new `DisplayOption` filter to `HabitListView` with `All` and `Not Done` modes.
- Wired the filter into the existing habits list pipeline so `Not Done` excludes habits already completed on the selected date.
- Added a nested `Show` submenu to the existing toolbar menu, alongside the existing sort and group controls.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

The Habits screen can now switch between showing all habits for the selected day and only the habits that are not yet completed.

⸻

🗓 2026-04-07 · 11:31 (GMT+2)

🔹 Request

User requested that if a habit has no reminder time, the streak/flame should show next to the recurrence label instead.

🔧 Changes Made
- Updated `HabitListView` so no-time habits render the flame inline beside the recurrence metadata line.
- Removed the separate no-time streak-only line, while keeping the existing time-plus-flame layout for habits that do have reminder times.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Habits without reminder times now show streak information next to their recurrence label, keeping the metadata tighter and more coherent.

⸻

🗓 2026-04-07 · 11:29 (GMT+2)

🔹 Request

User requested that all habit rows should show how often they recur.

🔧 Changes Made
- Updated `HabitListView` to always show the recurrence metadata line for every habit row.
- Removed the conditional logic that hid the schedule line for every-day habits.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Every habit row now shows its recurrence label, including every-day habits.

⸻

🗓 2026-04-07 · 11:27 (GMT+2)

🔹 Request

User requested that the most recent habit-row metadata change be reverted.

🔧 Changes Made
- Reverted the always-visible repeat line in `HabitListView`, so the schedule label is no longer forced to show for every-day habits.
- Removed the fixed reserved placeholder slots that were added for the reminder/streak and priority metadata rows.
- Restored the previous conditional metadata layout.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

The habit row metadata behavior is back to the prior version before the always-show repeat and fully fixed-slot metadata layout change.

⸻

🗓 2026-04-07 · 11:25 (GMT+2)

🔹 Request

User reported that the row still shifted and also pointed out that repeating habits were not showing their repeat label when set to every day.

🔧 Changes Made
- Updated `HabitListView` to always show the repeat metadata line, including `Every day`.
- Removed the conditional omission of the reminder/streak metadata row and replaced it with a permanently reserved fixed-height slot.
- Made the priority metadata row keep a fixed slot as well, so the metadata stack height stays stable across completion/streak changes.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Habit rows now keep a stable metadata height, and every-day habits explicitly show their repeating schedule instead of hiding it.

⸻

🗓 2026-04-07 · 11:23 (GMT+2)

🔹 Request

User reported that habits without reminder times still shifted when the streak/flame appeared.

🔧 Changes Made
- Extracted the reminder/streak metadata into a dedicated stable line in `HabitListView`.
- Added a fixed-height placeholder for the reminder/streak slot when needed so no-time habits do not gain a new line only when the flame appears.
- Kept the visible flame inline when present, while preventing the row from reflowing around that state change.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Habits without reminder times should no longer jump in height or layout when a streak appears.

⸻

🗓 2026-04-07 · 11:20 (GMT+2)

🔹 Request

User requested that row icons should be removed for now and that the completed-row tint should also be removed.

🔧 Changes Made
- Removed the leading habit icon tile from habit rows in `HabitListView`.
- Removed the rounded completed-row background tint.
- Kept the separate completion checkbox and existing metadata layout intact.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Habit rows now use a simpler text-and-checkbox layout without icon tiles or row tinting.

⸻

🗓 2026-04-07 · 11:15 (GMT+2)

🔹 Request

User requested that the completed-state background should return on the icon, and suggested tinting the whole row.

🔧 Changes Made
- Restored the tinted completed-state fill on the habit icon tile in `HabitListView`.
- Added a subtle rounded full-row accent tint for completed habits so the checked state is expressed across the row instead of relying on stronger icon-only emphasis.
- Kept the row height and icon frame unchanged while adding the tint treatment.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Completed habits now have both the icon background tint and a light full-row accent tint, making the checked state clearer without reintroducing the earlier size/height issues.

⸻

🗓 2026-04-07 · 11:18 (GMT+2)

🔹 Request

User reported that checking a habit still increased the row height.

🔧 Changes Made
- Removed the completed-only blank metadata spacer from `HabitListView`.
- Kept the priority metadata row only when there is actual priority content to show.
- Left the icon tile size unchanged; the height shift source was the extra conditional spacer, not the icon frame itself.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Checking a habit should no longer add an empty line that increases the row height.

⸻

🗓 2026-04-07 · 11:16 (GMT+2)

🔹 Request

User reported that the habit icon looked bigger when the habit was checked.

🔧 Changes Made
- Removed the filled checked-state background treatment from the habit icon tile in `HabitListView`.
- Kept the icon tile at the same fixed size and switched completion indication to a subtle accent border instead of a larger-feeling filled tile.
- Restored consistent icon foreground styling so checked and unchecked states retain the same visual weight.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

The icon now keeps the same visual size when a habit is checked instead of appearing to grow from the checked-state styling.

⸻

🗓 2026-04-07 · 11:14 (GMT+2)

🔹 Request

User disliked the icon-tile completion control and requested that it be reverted without making the row feel bigger.

🔧 Changes Made
- Reverted the icon-tile completion toggle in `HabitListView`.
- Restored a separate checkbox control for completion.
- Reduced the checkbox visual size and reserved width so the row stays tighter than before and does not feel enlarged.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

The leading icon and completion control are separated again, but the checkbox now takes less space so the row remains more compact.

⸻

🗓 2026-04-07 · 11:09 (GMT+2)

🔹 Request

User chose the icon-as-completion-control approach to replace the separate icon and checkbox controls.

🔧 Changes Made
- Removed the standalone checkbox from habit rows in `HabitListView`.
- Made the icon tile itself the completion toggle action with the existing spring animation.
- Added a small checkmark overlay on completed icon tiles so completion state remains obvious without introducing a second left-side control.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Each habit row now has a single coherent leading control: the icon tile identifies the habit and also acts as the completion toggle, which reduces visual clutter and removes the awkward side-by-side icon/checkbox pairing.

⸻

🗓 2026-04-07 · 11:12 (GMT+2)

🔹 Request

User requested that sort and group controls should be placed into nested menus.

🔧 Changes Made
- Replaced the separate sort and group toolbar buttons in `HabitListView` with a single parent toolbar menu.
- Added nested `Sort By` and `Group By` submenus inside that parent menu, preserving the existing option sets and current-selection checkmarks.
- Updated the toolbar icon to reflect the combined organization/filter action.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

The Habits toolbar is cleaner, while sort and grouping options remain fully available under one nested menu.

⸻

🗓 2026-04-07 · 11:06 (GMT+2)

🔹 Request

User requested that the streak/flame indicator should appear next to the reminder time instead of on its own separate metadata row.

🔧 Changes Made
- Updated `HabitListView` so streak metadata now renders inline with the reminder time row when a reminder exists.
- Added a fallback so habits with a streak but no reminder still show the flame on its own metadata line.
- Simplified the separate metadata summary row so it now only handles priority spacing/stability instead of duplicating streak output.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

The streak indicator now sits next to the time where it reads more naturally as part of the habit’s active-day status, while still remaining visible for habits without reminder times.

⸻

🗓 2026-04-07 · 11:10 (GMT+2)

🔹 Request

User requested that `Import From Reminders` and `Random Nudges` should still be visible when no habits exist, and then move into the toolbar once habits are present.

🔧 Changes Made
- Updated `HabitListView` empty state to include direct actions for `Import From Reminders` and `Random Nudges`.
- Removed the inline top-of-list entry cards for those actions from the populated habits list.
- Added a toolbar menu for existing-habit states so `Import From Reminders` and `Random Nudges` remain accessible without taking permanent list space.
- Updated empty-state copy to reflect all three starting paths: add a habit, import reminders, or use random nudges.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

The two secondary entry points are now available even before the first habit exists, and they move into the toolbar after the user has habits so the main list stays cleaner.

⸻

🗓 2026-04-07 · 11:07 (GMT+2)

🔹 Request

User reported that habit rows still looked odd because empty space was being reserved where the streak/flame metadata would go.

🔧 Changes Made
- Refined the conditional metadata summary row in `HabitListView` so blank spacer height is only reserved when the habit is completed on the selected day.
- Removed the always-empty visual gap for normal rows that have no visible priority or streak metadata.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Rows without visible streak or priority metadata no longer show an unnecessary empty gap, while completed-row state changes still remain visually stable.

⸻

🗓 2026-04-07 · 11:05 (GMT+2)

🔹 Request

User requested that habit titles should wrap instead of truncating.

🔧 Changes Made
- Updated the habit title text in `HabitListView` to allow two lines.
- Removed the shrinking behavior and switched the title to multiline leading-aligned layout so longer habit names remain readable.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Longer habit names now wrap onto a second line instead of compressing or being cut off.

⸻

🗓 2026-04-07 · 11:03 (GMT+2)

🔹 Request

User reported that the streak/flame metadata on habit rows was indented incorrectly.

🔧 Changes Made
- Reworked the bottom metadata row in `HabitListView` so hidden priority metadata no longer reserves horizontal layout space with `.opacity(0)`.
- Replaced the always-laid-out priority/streak `HStack` with a conditional summary row that keeps the row height stable while left-aligning whichever metadata is actually visible.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

The streak/flame indicator now aligns flush with the other metadata instead of appearing indented when no priority badge is shown.

⸻

🗓 2026-04-07 · 11:00 (GMT+2)

🔹 Request

User requested an `Import All` option for repeating reminders.

🔧 Changes Made
- Added an `Import All` toolbar action to `RemindersImportView` to bulk import every currently visible repeating reminder in one step.
- Added a `Select All` / `Clear All` control in the explanatory section so users can quickly bulk-select or reset the visible reminder list without losing the manual selection flow.
- Reused the existing import pipeline so duplicate skipping, result messaging, and reminder-to-habit conversion stay consistent between manual import and bulk import.

Files modified:
- `Habit Tracker/RemindersImportView.swift`

✅ Result

Users can now import all visible repeating reminders immediately, while still having a quick-select option when they only want to review the list before importing.

⸻

🗓 2026-04-07 · 10:56 (GMT+2)

🔹 Request

User reported that some imported every-day reminders, such as 06:30 daily items, were still not being scheduled even though others were.

🔧 Changes Made
- Optimized `HabitReminderScheduler` so `Every day` habits now use a single repeating daily notification request instead of seven separate weekday-based repeating requests.
- Updated reminder verification in `ScheduledNotificationsView` so every-day habits now correctly expect one pending iOS request rather than seven.
- Kept weekday and other custom schedules on per-weekday repeating requests, since those still require distinct weekday matching.

Files modified:
- `Habit Tracker/HabitDataServices.swift`
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

Every-day habits now use far fewer pending notification slots, which reduces the chance that imported daily reminders fail to schedule because the iOS local notification queue is saturated.

⸻

🗓 2026-04-07 · 10:49 (GMT+2)

🔹 Request

User reported that many reminder rows incorrectly showed `Pending in iOS does not match expected schedule` with `none`, indicating the first verification pass was over-reporting mismatches.

🔧 Changes Made
- Corrected habit reminder verification in `ScheduledNotificationsView` to match how standard reminders are actually scheduled: one repeating request per active weekday.
- Replaced the fragile pending-vs-expected today-date comparison for normal reminders with a request-count check against the expected number of scheduled repeating notifications.
- Kept next-date comparison only for interval-based reminders, where concrete future request dates are the actual scheduling model.
- Updated the row status text to report how many iOS requests are scheduled instead of incorrectly implying `none` for valid repeating reminders.

Files modified:
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

Standard imported and non-imported habit reminders should no longer be falsely flagged as missing just because iOS only exposes limited repeating-trigger date information.

⸻

🗓 2026-04-07 · 10:45 (GMT+2)

🔹 Request

User requested a stricter Reminders screen that verifies expected habit reminders against the actual pending iOS notification queue.

🔧 Changes Made
- Extended habit reminder grouping in `ScheduledNotificationsView` to compute both expected reminder times from the saved habit model and pending reminder times from `UNUserNotificationCenter`.
- Added per-row mismatch detection by comparing expected-vs-pending times at minute precision for both today’s remaining reminders and the next upcoming reminder.
- Updated reminder rows to show `Expected today` and `Pending in iOS` information, plus a visible warning when the pending iOS queue does not match the app’s expected schedule.

Files modified:
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

The Reminders screen now distinguishes between what the app expects to send and what iOS currently has pending, making imported-reminder scheduling issues visible instead of implicit.

⸻

🗓 2026-04-07 · 10:38 (GMT+2)

🔹 Request

User requested that reminder imports should stop creating "every X days" style habits and instead import those reminders as normal every-day habits.

🔧 Changes Made
- Removed interval schedule creation from the reminder import path.
- Simplified imported reminder candidate data so it no longer carries interval metadata for import.
- Changed imported schedule labeling so daily reminders with interval > 1 are shown/imported as `Every day`.
- Kept interval scheduling support in the app runtime for existing interval habits, but stopped generating that schedule type from Apple Reminders import.

Files modified:
- `Habit Tracker/HabitDataServices.swift`

✅ Result

Imported reminders that would previously have become "every X days" habits now import as standard every-day habits instead.

⸻

🗓 2026-04-07 · 10:35 (GMT+2)

🔹 Request

User reported that the forced reminder refresh approach was janky and requested that the imported reminders path be fixed properly instead.

🔧 Changes Made
- Removed the foreground-triggered full reminder rebuild from `HabitListView`.
- Removed the Reminders screen auto-rebuild-on-appear/foreground behavior from `ScheduledNotificationsView`.
- Changed reminder import finalization to run one full `HabitReminderScheduler.refreshAllReminders(in:)` after imported habits are saved and interval metadata is applied, instead of scheduling imported habits individually in a piecemeal way.

Files modified:
- `Habit Tracker/HabitListView.swift`
- `Habit Tracker/ScheduledNotificationsView.swift`
- `Habit Tracker/HabitDataServices.swift`

✅ Result

The UI no longer thrashes from aggressive rebuilds, and imported reminders now trigger a single proper full reschedule from the saved app model immediately after import, which is the cleaner place to solve the issue.

⸻

🗓 2026-04-07 · 10:34 (GMT+2)

🔹 Request

User reported inconsistent reminder display where one app load showed none of the expected items and the next load showed all of them, indicating a refresh timing issue.

🔧 Changes Made
- Added an optional completion callback to `HabitReminderScheduler.refreshAllReminders(in:)`.
- Updated `ScheduledNotificationsView` to trigger a full reminder rebuild on appear and on app foreground, then fetch pending notifications only after the rebuild callback returns.
- Added a short main-thread delay before reading pending notifications to reduce race conditions with `UNUserNotificationCenter` request registration.

Files modified:
- `Habit Tracker/HabitDataServices.swift`
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

The Reminders screen now waits for reminder rescheduling to finish before reading pending notifications, reducing the inconsistent “shows none, then shows all on next reload” behavior.

⸻

🗓 2026-04-07 · 10:32 (GMT+2)

🔹 Request

User observed that imported reminder notifications were corrected by toggling reminders off and on, and requested the app to do the equivalent automatically when returning to the foreground.

🔧 Changes Made
- Updated the app foreground activation path in `HabitListView` to call `HabitReminderScheduler.refreshAllReminders(in:)` whenever the app becomes active.
- Kept the existing daily habit-state sync in the same activation flow.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Returning to the app now triggers the same full reminder rebuild that previously only happened when notifications were manually toggled off and on, reducing drift in pending reminder schedules.

⸻

🗓 2026-04-07 · 10:29 (GMT+2)

🔹 Request

User reported that the Scheduled Notifications screen was still not showing everything that should be coming later today for habit reminders.

🔧 Changes Made
- Changed today-reminder calculation in `ScheduledNotificationsView` to derive upcoming-today reminder times directly from each habit’s saved reminder time and active schedule.
- Stopped relying solely on `UNCalendarNotificationTrigger.nextTriggerDate()` for the per-row `Today:` line, since repeating calendar notification requests can under-report what should still appear as due today.
- Reused `HabitScheduleResolver.isActive(...)` so weekday and interval schedule logic remains consistent with the rest of the app.

Files modified:
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

The `Today:` line for habit reminders should now reflect all habits that are actually scheduled to fire later today based on the app’s own schedule model, rather than depending on notification-trigger introspection alone.

⸻

🗓 2026-04-07 · 09:43 (GMT+2)

🔹 Request

User reported that habit reminders were not showing all reminder times for the rest of the day.

🔧 Changes Made
- Extended habit reminder grouping in the Reminders screen to collect all upcoming trigger dates for today.
- Updated habit reminder rows to show a `Today:` line listing the remaining reminder times for the current day.
- Kept the existing `Next alert ...` summary line while adding the full remaining-day schedule.

Files modified:
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

Habit reminder rows now show the complete set of remaining reminder times for today instead of only the next alert.

⸻

🗓 2026-04-07 · 09:42 (GMT+2)

🔹 Request

User requested that random nudges should show when all nudges are going to fire for the day.

🔧 Changes Made
- Extended random nudge grouping models to capture all pending trigger dates that fall on today.
- Updated random nudge rows in both the Habits nudge manager and the Reminders screen to show a `Today:` line listing all scheduled fire times for the current day.
- Kept the existing single `Next nudge ...` summary line alongside the daily full-time list.

Files modified:
- `Habit Tracker/RandomNudgesView.swift`
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

Each random nudge now shows the complete set of its scheduled fire times for today, making the day’s nudge plan visible at a glance.

⸻

🗓 2026-04-07 · 09:41 (GMT+2)

🔹 Request

User reported that the greyed-out completed habit state was too faint and made the habit hard to see.

🔧 Changes Made
- Increased completed habit title contrast so completed rows remain readable instead of fading too far toward secondary text color.
- Softened the strikethrough tint while keeping the completed affordance.
- Adjusted metadata line fading for completed rows to reduce over-dimming.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Completed habits still look completed, but they no longer disappear visually into the background.

⸻

🗓 2026-04-07 · 09:41 (GMT+2)

🔹 Request

User requested that today’s random nudge times should stay locked once generated.

🔧 Changes Made
- Added `RandomNudgeGeneratedScheduleStore` to persist generated random nudge fire times per nudge and per day.
- Updated random nudge scheduling to reuse stored day times instead of re-randomizing on every refresh/reload.
- Kept regeneration behavior only for new unseen days.
- Added cleanup so deleting or editing a nudge clears its stored generated schedule before rescheduling.

Files modified:
- `Habit Tracker/HabitDataServices.swift`
- `Habit Tracker/RandomNudgesView.swift`
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

Random nudge times now stay stable for a given day once generated, instead of changing whenever reminders are refreshed. Editing or removing a nudge correctly clears and regenerates its schedule.

⸻

🗓 2026-04-07 · 09:40 (GMT+2)

🔹 Request

User requested the ability to edit random nudges.

🔧 Changes Made
- Added update support to `RandomNudgeStore` so existing nudges can be modified in place without creating duplicates.
- Reused the existing random nudge form as both create and edit flow, with prefilled values when editing.
- Added tap-to-edit and swipe/context-menu edit actions to random nudge rows in both the Habits nudge manager and the Reminders screen.
- Kept nudge rescheduling behavior on save so edited wake windows/counts immediately regenerate scheduled notifications.

Files modified:
- `Habit Tracker/HabitDataServices.swift`
- `Habit Tracker/RandomNudgesView.swift`
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

Random nudges can now be edited from either place they appear, with changes saved back to the existing nudge and its notifications rescheduled accordingly.

⸻

🗓 2026-04-07 · 09:39 (GMT+2)

🔹 Request

User requested that newly created habits should not appear on previous days, only from the day they were created onward.

🔧 Changes Made
- Used the existing `Habit.timestamp` field as the habit creation/start date.
- Set `timestamp = Date()` when creating a new habit.
- Set `timestamp = Date()` when importing reminders as new habits so imported habits also start from the import date.
- Added `HabitScheduleResolver.existsOnOrBefore(...)` and integrated it into active-day evaluation so habits are inactive before their creation date.
- Updated the habits list filtering to exclude habits from past selected dates before they existed.

Files modified:
- `Habit Tracker/HabitDataServices.swift`
- `Habit Tracker/NewHabitView.swift`
- `Habit Tracker/HabitListView.swift`

✅ Result

Newly created or imported habits now only show up from their creation/import date onward and no longer appear when viewing earlier days.

⸻

🗓 2026-04-07 · 09:38 (GMT+2)

🔹 Request

User requested that random nudges should show only one upcoming fire time instead of four.

🔧 Changes Made
- Removed the extra stored/displayed upcoming trigger date list from random nudge grouping models.
- Reverted both random nudge list UIs back to showing only the single `Next nudge ...` line.

Files modified:
- `Habit Tracker/RandomNudgesView.swift`
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

Random nudges now display only one upcoming fire time again, keeping the rows shorter and simpler.

⸻

🗓 2026-04-07 · 09:37 (GMT+2)

🔹 Request

User requested that marking a habit complete should not cause layout shift by suddenly showing a streak line.

🔧 Changes Made
- Updated habit row metadata layout so the priority/streak line is always present with a fixed height.
- Replaced conditional insertion/removal of priority/streak metadata with opacity-based visibility, preserving row height when streak becomes non-zero.

Files modified:
- `Habit Tracker/HabitListView.swift`

✅ Result

Completing a habit no longer changes the row height just because a streak indicator appears, reducing visible layout shift in the habits list.

⸻

🗓 2026-04-07 · 09:35 (GMT+2)

🔹 Request

User asked to restore autocorrect on the habit name field.

🔧 Changes Made
- Removed the explicit autocorrect disable modifier from the new habit name text field.
- Kept the other field stability adjustments in place.

Files modified:
- `Habit Tracker/NewHabitView.swift`

✅ Result

The habit name field now uses normal iOS autocorrect behavior again while keeping the recent sizing/stability changes.

⸻

🗓 2026-04-07 · 09:33 (GMT+2)

🔹 Request

User provided likely root-cause guidance for the text-field shrink issue and requested the input to be stabilized without keeping the worse custom layout.

🔧 Changes Made
- Updated the new habit name text field to use a fixed `44` point height.
- Moved stable inner padding/background styling directly onto the field so the focus/keyboard transition does not depend on intrinsic height changes.
- Used a rounded rectangle background with grouped-system fill to keep the field visually stable inside the native `Form` layout.

Files modified:
- `Habit Tracker/NewHabitView.swift`

✅ Result

The create screen stays on the native `Form` layout, while the habit name input now has a fixed visual height intended to prevent the focus-time shrinking behavior caused by keyboard/layout transitions.

⸻

🗓 2026-04-07 · 09:32 (GMT+2)

🔹 Request

User requested that random nudges in the reminders views should show the four times they are going to fire.

🔧 Changes Made
- Extended random nudge grouping models in both the Habits nudge manager and the Reminders screen to store the next four upcoming trigger dates.
- Updated random nudge row UI to show a `Next 4:` line with the next four scheduled fire times.
- Kept the existing relative `Next nudge ...` summary while adding the explicit upcoming timestamps.

Files modified:
- `Habit Tracker/RandomNudgesView.swift`
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

Random nudges now show the next four scheduled fire times in both places they appear, making the random schedule more transparent to the user.

⸻

🗓 2026-04-07 · 09:29 (GMT+2)

🔹 Request

User reported that the create screen looked worse after the custom layout rewrite and requested that it be put back.

🔧 Changes Made
- Reverted `NewHabitView` from the custom `ScrollView`/card layout back to the prior native `Form`-based structure.
- Kept the simpler focus handling changes in place while removing the visual layout regression introduced by the rewrite.

Files modified:
- `Habit Tracker/NewHabitView.swift`

✅ Result

The create screen is back on the more familiar native `Form` presentation instead of the custom card layout that made the screen feel worse.

⸻

🗓 2026-04-07 · 09:28 (GMT+2)

🔹 Request

User reported that the Reminders screen was visually broken, with duplicate reminder rows and missing/incorrect schedule text after recent reminder scheduling changes.

🔧 Changes Made
- Reworked reminder grouping in `ScheduledNotificationsView` to use Core Data habits as the source of truth instead of parsing/grouping notification identifiers directly.
- Matched pending notification requests back to each habit using the habit’s base reminder identifier prefix.
- Replaced inferred weekday display with `HabitScheduleResolver.label(for:)` so the Reminders screen uses the same schedule text as the rest of the app.
- Removed the old identifier-parsing weekday/base grouping helpers that were causing duplicate and malformed rows when interval-generated identifiers were present.

Files modified:
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

The Reminders screen now shows one row per habit reminder, with stable schedule text and without the duplicate/broken entries caused by mixed notification identifier formats.

⸻

🗓 2026-04-07 · 09:27 (GMT+2)

🔹 Request

User reported the input shrink glitch still persisted and requested a proper fix.

🔧 Changes Made
- Replaced the create screen container from SwiftUI `Form` to a stable `ScrollView` + grouped card sections to avoid focus-time form row relayout artifacts.
- Kept the same creation fields and behavior while restructuring layout blocks for visual stability.
- Added reusable internal `sectionCard` helper for consistent native-style grouped sections.
- Enabled interactive keyboard dismissal on scroll for smoother input behavior.

Files modified:
- `Habit Tracker/NewHabitView.swift`

✅ Result

The create screen no longer relies on `Form` focus layout behavior, which should remove the persistent text-field shrink/jitter when focusing inputs.

⸻

🗓 2026-04-07 · 09:26 (GMT+2)

🔹 Request

User reported the input shrink visual glitch still occurred after the initial focus stabilization tweak.

🔧 Changes Made
- Removed explicit `@FocusState` management from the habit name text field.
- Removed `.focused(...)` binding and focus submit reset logic to avoid SwiftUI focus-transition layout behavior in the create form.

Files modified:
- `Habit Tracker/NewHabitView.swift`

✅ Result

The create field now uses plain native text-field focus handling without custom focus state, which should eliminate the remaining focus-induced shrink artifact.

⸻

🗓 2026-04-07 · 09:25 (GMT+2)

🔹 Request

User reported a visual glitch where input fields appeared to shrink slightly when focused.

🔧 Changes Made
- Removed auto-focus-on-appear behavior from the new habit name field to avoid immediate form relayout/focus transition artifacts.
- Disabled implicit animation on the habit name text field focus transaction to reduce focus-state size jitter in SwiftUI `Form`.

Files modified:
- `Habit Tracker/NewHabitView.swift`

✅ Result

Focusing the habit name input should now feel more stable, with reduced visual shrinking/jitter during focus transitions.

⸻

🗓 2026-04-07 · 09:22 (GMT+2)

🔹 Request

User reported that an imported weekday reminder ("send time sheet") showed as weekdays in edit, but the Reminders screen displayed Tue-Fri.

🔧 Changes Made
- Updated reminders grouping display logic to use each habit’s saved schedule mask as the primary source of truth for weekday labels.
- Added lookup mapping from habit base reminder identifier -> computed weekday list from `activeDaysMask`.
- Kept notification-request-derived weekdays as fallback when no matching habit is found.

Files modified:
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

The Reminders screen now reflects the habit’s configured schedule consistently, so weekday habits display correctly as weekdays rather than missing-day subsets.

⸻

🗓 2026-04-07 · 09:17 (GMT+2)

🔹 Request

User requested that the reminders import list should only show reminders that have repeats.

🔧 Changes Made
- Updated reminder import loading to filter out non-recurring reminders.
- Added repeat-rule check so only reminders with at least one recurrence rule are displayed in import sections.

Files modified:
- `Habit Tracker/RemindersImportView.swift`

✅ Result

The import screen now only displays repeating reminders, reducing noise and matching the intended use case.

⸻

🗓 2026-04-07 · 09:15 (GMT+2)

🔹 Request

User requested that reminders recurring every day should import as an every-day habit schedule.

🔧 Changes Made
- Updated reminder import schedule detection to explicitly treat daily recurrence with interval `1` as `Every day`.
- Updated imported active-day mask mapping so these reminders are assigned `HabitSchedule.allDaysMask` instead of falling back to a single due weekday.
- Updated imported schedule label rendering for this case to show `Every day`.

Files modified:
- `Habit Tracker/HabitDataServices.swift`

✅ Result

Reminders that recur every day now import with an every-day schedule label and behavior, matching the original reminder recurrence.

⸻

🗓 2026-04-07 · 09:14 (GMT+2)

🔹 Request

User requested support for reminders that repeat every 3 days during import.

🔧 Changes Made
- Added interval-based schedule support for imported daily recurrence rules with interval > 1 (e.g. every 3 days).
- Added per-habit interval persistence (`HabitIntervalScheduleStore`) using habit object URI keys.
- Added schedule resolver logic (`HabitScheduleResolver`) to evaluate habit activity from interval schedules when present.
- Updated reminder import mapping to detect daily interval recurrences from EventKit and store interval/anchor information.
- Updated reminder import schedule labels to display `Every N days` for interval recurrences.
- Updated habit reminder scheduling to support interval habits by creating rolling non-repeating notifications for matching interval days.
- Extended reminder cleanup to remove interval-generated notification identifiers.
- Updated Habits list filtering and app completion action total-count logic to respect interval schedule resolution.
- Added interval schedule cleanup when deleting habits.
- Fixed interval persistence timing to store interval metadata after Core Data save so permanent object IDs are used.

Files modified:
- `Habit Tracker/HabitDataServices.swift`
- `Habit Tracker/HabitListView.swift`
- `Habit Tracker/HabitDetailView.swift`
- `Habit Tracker/Habit_TrackerApp.swift`

✅ Result

Imported reminders with patterns like "every 3 days" are now represented as interval habits, display the correct schedule label, participate in active-day logic correctly, and receive notifications on the correct interval cadence.

⸻

🗓 2026-04-07 · 09:11 (GMT+2)

🔹 Request

User reported that some imported reminders were being shown as "Every day" even when that schedule was incorrect.

🔧 Changes Made
- Updated reminder import schedule labeling logic to avoid defaulting non-recurring reminders to "Every day".
- Added fallback weekday extraction from full due-date components when `weekday` is not explicitly present.
- Changed non-recurring schedule label fallback to `No repeat` when no reliable weekday is available.
- Improved recurrence weekday label normalization by comparing weekday sets (order-independent) and then rendering in consistent Mon-Sun order.
- Updated import schedule-mask mapping to use the same due-date weekday fallback logic for more accurate habit schedule assignment.

Files modified:
- `Habit Tracker/HabitDataServices.swift`

✅ Result

Imported reminder rows should now show more accurate schedule labels and map one-off reminders to an appropriate day instead of incorrectly displaying/assuming "Every day".

⸻

🗓 2026-04-07 · 09:10 (GMT+2)

🔹 Request

User requested the create screen to look nicer and more native.

🔧 Changes Made
- Refined `NewHabitView` structure for a more native iOS form presentation with clearer section grouping and system icon headers.
- Moved create action to navigation bar toolbar (`Create`) and added a leading `Cancel` action to match native modal form patterns.
- Improved text-input ergonomics:
- Added word capitalization and disabled autocorrection for habit names.
- Added keyboard submit behavior and initial focus on habit name field.
- Switched schedule preset control to segmented style for quicker in-form selection.
- Kept reminder controls in-place but used compact time picker styling and clearer section footer guidance.
- Added section footers and labels for better hierarchy and readability.

Files modified:
- `Habit Tracker/NewHabitView.swift`

✅ Result

The habit creation experience now follows more standard native iOS form conventions with better visual hierarchy, more consistent action placement, and smoother data entry.

⸻

🗓 2026-04-07 · 09:06 (GMT+2)

🔹 Request

User requested an option to import reminders from the Apple Reminders app into Habit Tracker, showing reminder lists and reminders so users can select which items to import with their time and schedule.

🔧 Changes Made
- Added EventKit-based reminder import support with permission handling and a new reminders import screen.
- Added a new Habits-screen entry point: `Import From Reminders`.
- Implemented reminders browser grouped by reminder list, showing each reminder’s effective time and schedule.
- Added multi-select import flow to import selected reminders as habits.
- Mapped imported reminder metadata into habits:
- Reminder title -> habit name
- Reminder list title -> habit category
- Reminder due time -> habit reminder time
- Reminder recurrence/due weekday -> habit active day schedule mask
- Added duplicate-name skip logic during import and user-facing import result feedback.
- Added `NSRemindersUsageDescription` to app Info.plist for iOS permission prompt compliance.
- Added `RemindersImportView.swift` to the app target in the Xcode project.

Files modified:
- `Habit Tracker/HabitDataServices.swift`
- `Habit Tracker/HabitListView.swift`
- `Habit Tracker/RemindersImportView.swift`
- `Habit-Tracker-Info.plist`
- `Habit Tracker.xcodeproj/project.pbxproj`

✅ Result

Users can now open Habits, choose `Import From Reminders`, view reminders across all lists, select items, and import them as habits while preserving their reminder timing and schedule information.

⸻

🗓 2026-04-07 · 09:03 (GMT+2)

🔹 Request

User requested random nudges to be placed on the Habits screen because they were not easily discoverable in the Reminders area.

🔧 Changes Made
- Added a dedicated `RandomNudgesView` screen to manage random nudge items from within the Habits flow.
- Added a visible `Random Nudges` entry section at the top of the Habits screen that opens the random nudge manager.
- Wired sheet presentation from `HabitListView` so users can access random nudges directly from Habits.
- Registered the new `RandomNudgesView.swift` file in the Xcode project sources.

Files modified:
- `Habit Tracker/RandomNudgesView.swift`
- `Habit Tracker/HabitListView.swift`
- `Habit Tracker.xcodeproj/project.pbxproj`

✅ Result

Random nudges are now accessible directly from the Habits screen, making the feature discoverable and easier to use from the main daily workflow.

⸻

🗓 2026-04-07 · 08:59 (GMT+2)

🔹 Request

User requested a separate random nudge/reminder section where notifications are sent at random times within user-selected waking hours, with a user-selected number of nudges per day, for things like posture/stretch/water prompts. User also clarified these are not habits and do not have completions.

🔧 Changes Made
- Added a dedicated random nudge data model and local persistence store (`RandomNudge`, `RandomNudgeStore`) separate from habit data/completions.
- Implemented random nudge notification scheduling (`RandomNudgeScheduler`) that:
- Schedules notifications at randomized times within configured wake hours.
- Schedules as many nudges per day as the user selects.
- Generates upcoming notifications across a rolling 7-day horizon.
- Removes and refreshes nudge notifications safely by nudge ID.
- Integrated random nudge handling into global notification refresh/disable flows so habit reminders and random nudges stay in sync with app notification settings.
- Extended the Reminders screen with a clearly separate `Random Nudges` section and explanatory copy that nudges are separate from habits and do not track completions.
- Added an in-app “Add Random Nudge” flow with:
- Nudge title and optional message.
- Wake start/end time selection.
- Nudges-per-day selection.
- Validation for title and wake-window ordering.
- Added random nudge list rows showing cadence, wake window, and next nudge timing.
- Added remove actions for random nudge groups, deleting both persisted configuration and pending notifications.

Files modified:
- `Habit Tracker/HabitDataServices.swift`
- `Habit Tracker/ScheduledNotificationsView.swift`

✅ Result

The app now supports a dedicated random nudges feature that is clearly separate from habits. Users can define wake hours and daily nudge frequency, receive notifications at random times within that window, and manage these nudges independently without affecting habit completion tracking.

⸻

🗓 2026-04-07 · 08:15 (GMT+2)

🔹 Request

User reported that reminders were still not being delivered as time-sensitive after the initial scheduling changes.

🔧 Changes Made
- Added a dedicated entitlements file with `com.apple.developer.usernotifications.time-sensitive = true`.
- Ensured the main app target uses this entitlements file for code signing in both Debug and Release configurations.
- Kept reminder content interruption level set to `.timeSensitive` and permission requests including `.timeSensitive`.

Files modified:
- `Habit Tracker/Habit Tracker.entitlements`
- `Habit Tracker.xcodeproj/project.pbxproj`

✅ Result

The app now has the required iOS entitlement for time-sensitive notifications, which is mandatory for system-level time-sensitive delivery behavior.

⸻

🗓 2026-04-07 · 11:05 (GMT+2)

🔹 Request

User requested tap-to-edit on habit rows and reminders to be scheduled as time-sensitive notifications.

🔧 Changes Made
- Added row-level tap handling in `HabitRow` to open habit editing directly.
- Converted `CheckBox` interaction to a plain `Button` so completion toggles remain explicit while enabling row-tap edit behavior.
- Updated notification permission request options to include `.timeSensitive` in `HabitReminderScheduler.ensureAuthorization`.

Files modified:
- `Habit Tracker/HabitListView.swift`
- `Habit Tracker/HabitDataServices.swift`

✅ Result

Tapping a habit row now opens edit mode, and reminder authorization now explicitly requests time-sensitive notification capability so scheduled reminders can use `.timeSensitive` interruption level.
